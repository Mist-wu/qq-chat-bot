# QQ Chat Bot

基于 NcatBot 的 QQ 聊天机器人项目。

## 📋 目录

- [环境要求](#环境要求)
- [Windows 安装](#windows-安装)
- [Linux 安装](#linux-安装)
- [运行](#运行)

---

## 环境要求

| 环境     | 要求                                            |
| -------- | -----------------------------------------------|
| Python   | 3.8 及以上（Windows 推荐 3.12）                 |
| 操作系统 | Windows 10/11 或 Linux（推荐 Ubuntu 24.04 LTS）  |
| QQ       | 最新版                                          |

> ⚠️ **注意**：请不要在云电脑上使用本项目，如需上云请使用 Linux 操作系统。

---

## Windows 安装

### 任务清单

- [ ] 正确安装 Python 3.12
- [ ] 通过 pip 安装 NcatBot
- [ ] 安装最新版 QQ
- [ ] 运行代码并扫码登录

### 1. 安装 Python

前往 [Python 官网](https://www.python.org/downloads/) 下载并安装 Python 3.12。

> 安装时请勾选 "Add Python to PATH"。

### 2. 安装 NcatBot

> ⚠️ **注意**：不要从 GitHub 上下载 `.zip` 压缩包；如果下载并解压过，请删掉解压出来的文件。

按下 `Win+R`，输入 `powershell` 并回车，打开终端，执行以下命令：

```powershell
pip install ncatbot -U -i https://mirrors.aliyun.com/pypi/simple
```

### 3. 安装最新版 QQ

> 💡 如果出现无法登录的问题，请先卸载 QQ，再去官网下载安装最新版。

1. 卸载电脑上已安装的 QQ
2. 前往 [QQ 官网](https://im.qq.com/) 下载安装最新版 QQ

---

## Linux 安装

推荐使用 **Ubuntu 24.04 LTS** 版本。

### 1. 安装基本工具

**Debian/Ubuntu：**

```bash
sudo apt-get update -y -qq && sudo apt-get -y -qq install curl sudo
```

**RPM/CentOS：**

```bash
sudo yum install -y curl sudo
```

### 2. 安装 NcatBot

> ⚠️ 请不要从 GitHub 上下载 `.zip` 压缩包，如果下载过，请删掉所有相关的文件。

```bash
# 进入 root 用户模式
sudo su

# 创建虚拟环境并安装
cd ~
python3 -m venv .ncatbot
. ~/.ncatbot/bin/activate
pip install ncatbot -U -i https://mirrors.aliyun.com/pypi/simple
```

---

## 运行

### 准备 QQ 号

需要两个 QQ 号：

| 角色     | 说明                                  |
| -------- | ------------------------------------- |
| **Bot**  | 由 NcatBot 控制，可以使用接口收发消息 |
| **root** | 由你控制，用于测试 Bot 的功能         |

> ⚠️ NcatBot 相当于一个电脑 QQ 客户端，因此在登录前，你需要退出 Bot 在其它电脑客户端上的登录。

### 开始运行

1. 新建一个工作目录（文件夹），如 `ncatbot`

2. 在文件夹中新建 `main.py` 文件，写入以下代码：

```python
# ========= 导入必要模块 ==========
from ncatbot.core import BotClient, PrivateMessage

# ========== 创建 BotClient ==========
bot = BotClient()

# ========= 注册回调函数 ==========
@bot.private_event()
async def on_private_message(msg: PrivateMessage):
    if msg.raw_message == "测试":
        await bot.api.post_private_msg(msg.user_id, text="NcatBot 测试成功喵~")

# ========== 启动 BotClient ==========
bot.run()
```

3. 在终端中运行：

**Windows：**

```powershell
python main.py
```

**Linux：**

```bash
python3 main.py
```

4. 在手机上登录 Bot，电脑按照提示输入 Bot QQ 号，然后手机扫码登录

5. 使用 root 账号向 Bot 发送消息 **"测试"**，收到回复即表示正常运行 🎉

---

## 📄 许可证

MIT License
